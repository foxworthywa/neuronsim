{
  "name": "neuronsim",
  "version": "0.2.0",
  "description": "Interactive, model-driven simulation that teaches how neurons signal (receive → integrate → fire → propagate → transmit).",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "npx --yes serve -l 8080 ."
  },
  "license": "MIT",
  "private": true
}
